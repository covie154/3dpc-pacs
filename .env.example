# Copy this file to .env and fill in real values before first run.
# .env is listed in .gitignore and must NEVER be committed.

# ── Public hostname ────────────────────────────────────────────────────────────
DOMAIN=pacs.example.com
CERTBOT_EMAIL=admin@example.com

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Internal DB password — not exposed outside Docker network
POSTGRES_PASSWORD=change-me-postgres-password

# ── Staff credentials (OE2 / REST API login) ──────────────────────────────────
ORTHANC_ADMIN_USER=admin
ORTHANC_ADMIN_PASSWORD=change-me-strong-password

# ── Orthanc → auth-service HTTP Basic Auth ────────────────────────────────────
# Same password must be set in both orthanc.json WebServicePassword fields
# AND as AUTH_SERVICE_PASSWORD below so auth-service can verify the caller.
AUTH_SERVICE_WEB_USER=share-user
AUTH_SERVICE_WEB_PASSWORD=change-me-service-password

# ── JWT signing key (auth-service only) ───────────────────────────────────────
# Generate with: openssl rand -hex 64
# Never put this in orthanc.json or any config file.
AUTH_SERVICE_SECRET_KEY=change-me-very-long-random-hex-generate-with-openssl-rand-hex-64

# ── Public URL roots (used by auth-service to build share link URLs) ──────────
PUBLIC_ORTHANC_ROOT=https://pacs.example.com/shares/
PUBLIC_LANDING_ROOT=https://pacs.example.com/shares/ui/app/token-landing.html
PUBLIC_OHIF_ROOT=https://pacs.example.com/ohif/
